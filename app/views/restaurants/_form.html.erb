<%= form_for(restaurant) do |f| %>
  <% if restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
      <% restaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :adress %>
    <%= f.text_area :adress %>
  </div>

  <div>
    <%= f.label :user %>
    <%= f.collection_select(:user_id, User.restaurant, :id, :email, include_blank: true) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
